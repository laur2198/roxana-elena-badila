---
import { SITE, SERVICES } from '../consts';
import Icon from './Icon.astro';
import { withBase } from '../lib/url';

// pathname include base la build (ex. /roxana-elena-badila/despre); comparăm cu withBase(href).
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => {
  const target = withBase(href).replace(/\/$/, '') || '/';
  return path === target || (href !== '/' && path.startsWith(target));
};
---
<nav class="site-nav">
  <div class="wrap nav-in">
    <a href={withBase('/')} class="brand">
      {SITE.name}<span>{SITE.subtitle}</span>
    </a>
    <button class="menu-btn" id="menuBtn" aria-label="Deschide meniul" aria-expanded="false" aria-controls="navLinks">
      <Icon name="menu" width={26} height={26} />
    </button>
    <div class="nav-links" id="navLinks">
      <a href={withBase('/despre')} class={isActive('/despre') ? 'active' : ''}>Despre mine</a>
      <div class="nav-drop">
        <a href={withBase('/servicii')} class={`drop-toggle ${isActive('/servicii') ? 'active' : ''}`}>
          Servicii
          <Icon name="chevron" width={15} height={15} />
        </a>
        <div class="drop-menu">
          <a href={withBase('/servicii')}>Toate serviciile</a>
          {SERVICES.map((s) => (
            <a href={withBase(`/servicii/${s.slug}`)}>{s.navLabel}</a>
          ))}
        </div>
      </div>
      <a href={withBase('/blog')} class={isActive('/blog') ? 'active' : ''}>Blog</a>
      <a href={withBase('/contact')} class="btn">
        <Icon name="calendar" width={17} height={17} />
        Programează o ședință
      </a>
    </div>
  </div>
</nav>

<script>
  const menuBtn = document.getElementById('menuBtn');
  const navLinks = document.getElementById('navLinks');
  if (menuBtn && navLinks) {
    menuBtn.addEventListener('click', () => {
      const open = navLinks.classList.toggle('open');
      menuBtn.setAttribute('aria-expanded', String(open));
    });
    navLinks.querySelectorAll('a').forEach((a) => {
      a.addEventListener('click', () => {
        navLinks.classList.remove('open');
        menuBtn.setAttribute('aria-expanded', 'false');
      });
    });
  }
</script>
